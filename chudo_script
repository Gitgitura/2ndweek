#!/bin/bash

# if удаляем
# then gzip и rm перемещает в директорию ~/RECYCLE (корзина), 
# else или  then автоматически удаляются файлы, которые находятся там более 7 дней

# Например, сначала твой скрипт проверяет, существует 
# ли директория ~/RECYCLE, и если нет, то создаёт её — это первая подзадача, и так далее.

#Или сначала проверяем что существует, затем сжимает и отправляет в корзину, потом удаляет. Значит надо
# понять:
- как можно проверить  существование
- как сжать
- как удалить с таймером
